---
- name: K8s demo
  hosts: localhost
  become: false
  tasks:
    - name: Create a K8s namespace
      kubernetes.core.k8s:
        name: team-infra
        api_version: v1
        kind: Namespace
        state: present
  
